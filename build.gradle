apply plugin: 'groovy'
apply plugin: 'eclipse'
apply from: 'mavenRelease.gradle'

group = "org.xtext"
version = "0.0.1"

repositories {
  mavenCentral()
}

dependencies {
  compile localGroovy()
  compile gradleApi()
}